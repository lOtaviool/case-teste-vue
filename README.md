# Case Teste Vue

## Sobre o projeto

Este projeto foi desenvolvido como parte do processo técnico da Fin-X.

A aplicação consiste em um front-end desenvolvido com Vue 3 utilizando TypeScript e Vite, consumindo uma API simulada através do JSON Server para gerenciamento dos dados durante o desenvolvimento.

## Tecnologias utilizadas

- Vue 3
- TypeScript
- Vite
- Bootstrap
- JSON Server

## Pré-requisitos

Antes de executar o projeto, certifique-se de possuir instalado em sua máquina:

- Node.js (versão 18 ou superior recomendada)
- npm ou Yarn
- Git

## Instalação

Clone o repositório:

```bash
git clone https://github.com/seu-usuario/case-teste-vue.git
```

Acesse a pasta do projeto:

```bash
cd case-teste-vue
```

Instale as dependências:

Com npm:

```bash
npm install
```

Ou utilizando Yarn:

```bash
yarn install
```

## Executando a aplicação

### Front-end

Para iniciar a aplicação Vue:

Com npm:

```bash
npm run dev
```

Ou com Yarn:

```bash
yarn dev
```

Após iniciar o projeto, a aplicação ficará disponível em:

```
http://localhost:5173
```

## Executando o servidor Mock

Este projeto utiliza o JSON Server para simular uma API REST.

Para iniciar o servidor:

Com npm:

```bash
npm run mock:server
```

Ou utilizando Yarn:

```bash
yarn mock:server
```

Por padrão, a API estará disponível em:

```
http://localhost:3001
```

## Autor

Desenvolvido por Luis Otávio Oliveira.
